<UserControl x:Class="BankoProject.Views.CountdownTimerControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BankoProject.Views"
             mc:Ignorable="d" 
             >
    <Viewbox>
        <DockPanel VerticalAlignment="Center" HorizontalAlignment="Left">
            <DockPanel DockPanel.Dock="Left" Name="OptionsPanel" Background="#FF8B8613" MinHeight="300" HorizontalAlignment="Left">
                <StackPanel Orientation="Vertical" Name="Optionspanel" DockPanel.Dock="Top" HorizontalAlignment="Left">
                    <Label>Antal Deltagere:</Label>
                    <TextBox>3</TextBox>
                </StackPanel>
                <StackPanel Orientation="Vertical" Name="StartStopReset" DockPanel.Dock="Bottom" Margin="0" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button BorderBrush="{x:Null}" Background="#FF8B8613">Start</Button>
                    <Button BorderBrush="{x:Null}" Background="#FF8B8613">Stop</Button>
                    <Button BorderBrush="{x:Null}" Background="#FF8B8613">Nulstil</Button>
                </StackPanel>
            </DockPanel>
            <DockPanel HorizontalAlignment="Right" VerticalAlignment="Center" DockPanel.Dock="Right">
                <ListBox ItemsSource="{Binding ButtonsList}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" HorizontalAlignment="Center" DockPanel.Dock="Top" VerticalAlignment="Center" Foreground="{x:Null}" BorderBrush="{x:Null}" Background="{x:Null}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical">
                                <Button Content="{Binding Path=Name}" Style="{DynamicResource MetroCircleButtonStyle}" Height="60" Width="60" Background="#FFBF5050" Foreground="{x:Null}"/>
                                <Label Content="{Binding Path=Time}" HorizontalAlignment="Center" VerticalAlignment="Top"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel IsItemsHost="True" Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Center"  />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                </ListBox>
            </DockPanel>
            <TextBox DockPanel.Dock="Bottom" HorizontalAlignment="Center" Width="62" Height="26" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" MaxHeight="26">00:00:000</TextBox>
        </DockPanel>
    </Viewbox>
</UserControl>
