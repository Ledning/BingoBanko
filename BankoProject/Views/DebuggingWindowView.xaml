<Window x:Class="BankoProject.Views.DebuggingWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BankoProject.Views"
        mc:Ignorable="d"
        Title="DebuggingWindowView" 
        Height="{Binding WinSngs.Height, Mode=TwoWay}" 
        Width="{Binding WinSngs.Width, Mode=TwoWay}"
        Left="{Binding WinSngs.Left, Mode=TwoWay}"
        Top="{Binding WinSngs.Top, Mode=TwoWay}"
        >
  <Grid>
    <Button x:Name="ShowWelcome" Margin="96,156,106,76" Content="WelcomeView">
      
    </Button>
    <Button x:Name="ShowControl" Margin="86,198,85,42" Content="ControlPanelView"/>
  </Grid>
</Window>
